{
  "name": "webgpu-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "🏆 Ultimate WebGPU Portfolio — Raw WebGPU + WGSL compute shaders, particles, bloom, interactive 3D scene. Zero frameworks.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
